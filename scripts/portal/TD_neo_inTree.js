function enter(pi) {
    var qq = 3719;
    if (pi.isQuestStarted(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("Quest complete");
    }
    qq = 3724;
    if (pi.isQuestStarted(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("Quest complete");
    }
    qq = 3730;
    if (pi.isQuestStarted(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("Quest complete");
    }
    qq = 3736;
    if (pi.isQuestStarted(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("Quest complete");
    }
    qq = 3742;
    if (pi.isQuestStarted(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("Quest complete");
    }
    qq = 3748;
    if (pi.isQuestStarted(qq)) {
	pi.forceCompleteQuest(qq);
	pi.playerMessage("Quest complete");
    }
}