/*
var status = 0;   
var mapid = PONER ID DEL MAPA ACA; //Borrar el "PONER ID DEL MAPA ACA" y poner el ID de un MAPA :D

function start() {   
    status = -1;   
    action(1, 0, 0);   
}   

function action(mode, type, selection) {  
        
    if (mode == -1) {   
        cm.dispose();   
    }   
    else {    
        if (status >= 2 && mode == 0) {   
            if(cm.getChar().isDonator() == true) { //checks for donator 
            cm.sendYesNo("Would you like to go to the donator map?");    
            cm.dispose();    
            cm.warp(mapid,0); 
        else if (status == 1) {  
        cm.sendOK("Alright then, happy Mapling!"); 
        cm.dispose(); 
        } 
       } 
     }  
     
     */